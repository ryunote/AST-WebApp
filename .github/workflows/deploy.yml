name: Deploy to Staging

on:
  push:
    branches:
      - main

jobs:
  test:
    uses: ./.github/workflows/test.yml

  deploy:
    needs: test
    runs-on: ubuntu-latest
    # ⚠️  本番環境で実装時にこのセクションを修正してください
    # - AWS ECR push
    # - ECS service update
    # - または Docker Swarm / K8s デプロイ
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push backend image (staging)
        run: |
          echo "⚠️  Staging デプロイ構成ここに追加"
          echo "例: aws ecr get-login-password | docker login ..."
          # docker build -t stock-backend:staging ./backend
          # docker push <AWS_ACCOUNT_ID>.dkr.ecr.<REGION>.amazonaws.com/stock-backend:staging

      - name: Build and push frontend image (staging)
        run: |
          echo "⚠️  Staging デプロイ構成ここに追加"
          # docker build -t stock-frontend:staging ./frontend
          # docker push <AWS_ACCOUNT_ID>.dkr.ecr.<REGION>.amazonaws.com/stock-frontend:staging

      - name: Update staging environment
        run: |
          echo "⚠️  Staging 環境の更新をここに追加"
          echo "例: aws ecs update-service --cluster staging --service stock-backend --force-new-deployment"
