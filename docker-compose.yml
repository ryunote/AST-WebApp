version: '3.8'

services:
  backend:
    build: ./backend
    container_name: stock-backend
    ports:
      - "8000:8000"
    volumes:
      # ローカルのコードをコンテナにマウント（コード変更を即座に反映させるため）
      - ./backend:/app
    # ホットリロードを有効にして起動
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload